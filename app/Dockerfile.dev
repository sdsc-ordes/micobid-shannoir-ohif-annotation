FROM node:20-alpine

WORKDIR /workspace/app

# Install dependencies layer (cached unless package.json changes)
COPY package*.json ./
RUN npm install

# Source is mounted via volume at runtime — no COPY needed for dev
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
