import { Component } from '@angular/core';

@Component({
  selector: 'app-projects-workspace',
  template: `<div class="stub">Projects workspace — implemented in Task 9.</div>`,
  styles: [`.stub { padding: 24px; color: var(--grey-mid); }`],
})
export class ProjectsWorkspace {}
